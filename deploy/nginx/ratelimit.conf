# -> /etc/nginx/conf.d/ratelimit.conf  (http context)
# Brute-force protection for the dashboard login endpoint.
# Used by the `location = /api/auth/login` block in throughputx.org.conf.
limit_req_zone $binary_remote_addr zone=login:10m rate=10r/m;
